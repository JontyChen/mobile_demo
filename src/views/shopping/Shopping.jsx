import React, { Component } from 'react'

export default class Shopping extends Component {
    render() {
        return (
            <div>
                {/* 首先是一个没有有商品时展示的页面，其次是有商品时展示另外一个页面。
                    建立一个仓库，将选中的商品放在其中，该组件读取里面的数据,查询是否有数据
                
                */}
                Shopping
            </div>
        )
    }
}
